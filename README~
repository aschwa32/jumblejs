Welcome to JumbleJS

Requirements
	-Unix/Linux operating system
	-NodeJS

Running the program
	-Navigate into directory
	-run the following command
		node jumble.js --word --dictionary(optional)
	-Example:
		node jumble.js dog
		node jumble.js dog /usr/share/dict/words
	

The program is written in node and defaults to the Native Unix dictionary located at "/usr/share/dict/words", though it can take an
extra parameter which specifies a different directory where a line separated dictionary exists.
